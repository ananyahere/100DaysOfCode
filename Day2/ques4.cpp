//Given an unsorted array arr[] of size N, rotate it by D elements (clockwise).

#include <bits/stdc++.h>
using namespace std;

int main()
{
  int d=3;
  int n=10;
  vector<vector <int>> mega_arr;
  for(int i=0; i<10; i++){
    int x;
    cin>>x;
    // mega_arr.push_back(x);
  }
}